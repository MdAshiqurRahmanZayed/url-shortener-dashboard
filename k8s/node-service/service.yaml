apiVersion: v1
kind: Service
metadata:
  name: node-service
  namespace: urlshortener
  labels:
    app: node-service
spec:
  selector:
    app: node-service
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
